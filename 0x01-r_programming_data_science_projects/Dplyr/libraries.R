# libraries
# (1) Tidy - reshape and tidy data
# (2) dplyr - manipulate data
# (3) tibble - Data frames
# (4) readr - read data
# (5) stringr - work with strings
# (6) forcats and category
# (7) lubridate - dates and time
# purrr (8) - functional programming and loops

# wide to long = pivot_longer(data, cols, names_to="name", values_to="value")

# long to wide = pivot_wider(data name_form="name", values_from="values")